# API base URL — leave empty for local dev (Vite proxy handles it)
# For production, set to your backend URL e.g.:
#   VITE_API_URL=https://evidex-7kr7.onrender.com
#   VITE_API_URL=https://yourdomain.com  (if backend is on same domain)
VITE_API_URL=
